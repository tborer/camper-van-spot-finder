node_modules/

# Generated by CI — the build scripts overwrite these during deploy.
# cell-towers.geojson is NOT committed (too large, always regenerated).
# ioverlander-spots.json IS committed as an empty placeholder so the app
# doesn't 404; CI overwrites it with real data in the Pages artifact.
src/data/cell-towers.geojson
